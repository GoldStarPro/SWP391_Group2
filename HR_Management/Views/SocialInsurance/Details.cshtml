@model HR_Management.Models.SocialInsurance

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Social Insurance List</h1>
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
        <li class="breadcrumb-item">Social Insurance</li>
        <li class="breadcrumb-item active" aria-current="page">Details</li>
    </ol>
</div>

<div class="card mb-3">
    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
        <h4 class="m-0 font-weight-bold text-primary">Social Insurace Details</h4>
    </div>
    <div class="card-body">
        <div class="form-group">
            <div class="card-body">
                <div class="form-group">
                    <label class="control-label">Issue Place</label>
                    <input asp-for="@Model.Issue_Place" disabled class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Registered Medical Facility</label>
                    <input asp-for="@Model.Registered_Medical_Facility" disabled class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Issue Date</label>
                    <input value="@Model.Issue_Date.ToString("dd/MM/yyyy")" disabled class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Notes</label>
                    <textarea asp-for="@Model.Notes" disabled class="form-control"></textarea>
                </div>
                <div class="form-group">
                    <a asp-action="Edit" asp-route-id="@Model.Social_Insurance_ID" class="btn btn-primary">Edit Info</a>
                    <a asp-action="Index" class="btn btn-secondary">Come Back</a>
                </div>
            </div>
        </div>
    </div>
</div>